---
// Header simplifié
---

<header id="main-header" class="bg-gradient-to-b from-blue-50/95 via-blue-50/90 to-white/95 backdrop-blur-sm border-b border-blue-100/50 sticky top-0 z-50 transition-all duration-300">
  <nav class="container mx-auto px-4 py-2 md:py-3">
    <div class="flex items-center justify-between max-w-[1040px] mx-auto">
      <a href="/" class="logo-link flex items-center space-x-2 text-xl font-bold">
        <img src="/images/logo_revise_tes_maths.png" alt="Logo Révise Tes Maths" class="logo-image h-8 w-8 object-contain" />
        <span class="logo-text">Révise Tes Maths</span>
      </a>
      
      <div class="hidden md:flex items-center space-x-6">
        <a href="/a-propos" class="nav-link text-sm">
          Qui suis-je ?
        </a>
        
        <a href="/blog" class="nav-link text-sm">
          Blog
        </a>
        
        <!-- Cours particuliers Mega Menu -->
        <div class="relative mega-menu-group" data-menu="cours">
          <button class="mega-menu-trigger nav-link text-sm flex items-center space-x-1" id="dropdown-cours-button">
            <span>Cours particuliers</span>
            <svg class="mega-menu-chevron w-4 h-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="mega-menu-panel hidden fixed z-50" id="dropdown-cours" data-trigger-id="dropdown-cours-button" style="left: 50%; transform: translateX(-50%);">
            <!-- Caret/Triangle -->
            <div class="mega-menu-caret" data-caret-for="dropdown-cours-button"></div>
            <!-- Panel Content -->
            <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden min-w-[720px]">
              <div class="p-8">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                  <!-- Modalités de cours -->
                  <div>
                    <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-4">Modalités de cours</h3>
                    <div class="space-y-2">
                      <a href="/cours-maths-visio" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Cours en visio</span>
                      </a>
                      <a href="/cours-paris" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 111.314 0z" />
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Cours à Paris</span>
                      </a>
                      <a href="/cours-collectifs-maths" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Cours collectifs</span>
                        <span class="text-xs bg-orange-100 text-orange-700 px-2 py-0.5 rounded-full font-semibold">-30%</span>
                      </a>
                    </div>
                  </div>

                  <!-- Collège -->
                  <div>
                    <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-4">Collège</h3>
                    <div class="space-y-2">
                      <a href="/3eme" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">3ème – Cours & Exercices</span>
                      </a>
                      <a href="/3eme/annales-brevet" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">3ème – Annales du brevet</span>
                      </a>
                    </div>
                  </div>

                  <!-- Lycée -->
                  <div>
                    <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider mb-4">Lycée</h3>
                    <div class="space-y-2">
                      <a href="/seconde" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Seconde – Cours & Exercices</span>
                      </a>
                      <a href="/premiere" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Première – Cours & Exercices</span>
                      </a>
                      <a href="/terminale" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">Terminale – Cours & Exercices</span>
                      </a>
                      <a href="/terminale/annales-bac" class="mega-menu-link flex items-center gap-3 px-3 py-2.5 rounded-lg hover:bg-gray-50 transition-colors group">
                        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        <span class="text-sm text-gray-700 group-hover:text-blue-600 font-medium">BAC – Annales</span>
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <a href="/tarifs" class="nav-link text-sm">
          Tarifs
        </a>

        <!-- Python (enseignement supérieur) -->
        <a 
          href="/enseignement-python" 
          class="nav-link text-sm"
        >
          Python
        </a>
      </div>
      
      <!-- CTA Button - appears on scroll -->
      <a 
        href="#contact" 
        id="header-cta"
        class="hidden md:inline-block bg-gradient-to-r from-blue-600 to-blue-700 text-white px-4 py-2 rounded-lg font-semibold text-sm hover:from-blue-700 hover:to-blue-800 transition-all duration-300 opacity-0 transform translate-x-4 shadow-md hover:shadow-lg"
      >
        Me contacter
      </a>
      
      <button 
        id="mobile-menu-button"
        class="md:hidden text-slate-600 hover:text-blue-600 transition-colors"
        aria-label="Menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
    
      <div id="mobile-menu" class="hidden md:hidden mt-4 pb-4 border-t border-blue-100/50">
      <div class="space-y-3 pt-4">
        <a href="/a-propos" class="nav-link-mobile block text-sm py-2">
          Qui suis-je ?
        </a>
        
        <a href="/blog" class="nav-link-mobile block text-sm py-2">
          Blog
        </a>
        
        <!-- Cours particuliers Mobile -->
        <div class="mobile-dropdown">
          <button class="nav-link-mobile block text-sm py-2 w-full text-left flex items-center justify-between" id="mobile-dropdown-cours-button">
            <span>Cours particuliers</span>
            <svg class="w-4 h-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="hidden pl-4 mt-2 space-y-2" id="mobile-dropdown-cours">
            <a href="/cours-maths-visio" class="block text-sm py-1 text-gray-600 pl-4">Cours en visio</a>
            <a href="/cours-paris" class="block text-sm py-1 text-gray-600 pl-4">Cours à Paris</a>
            <a href="/cours-collectifs-maths" class="block text-sm py-1 text-gray-600 pl-4">Cours collectifs</a>

            <div class="text-xs font-semibold text-gray-500 uppercase mb-1 mt-3">Collège</div>
            <a href="/3eme" class="block text-sm py-1 text-gray-600 pl-4">3ème – Cours & Exercices</a>
            <a href="/3eme/annales-brevet" class="block text-sm py-1 text-gray-600 pl-4">3ème – Annales du brevet</a>

            <div class="text-xs font-semibold text-gray-500 uppercase mb-1 mt-3">Lycée</div>
            <a href="/seconde" class="block text-sm py-1 text-gray-600 pl-4">Seconde – Cours & Exercices</a>
            <a href="/premiere" class="block text-sm py-1 text-gray-600 pl-4">Première – Cours & Exercices</a>
            <a href="/terminale" class="block text-sm py-1 text-gray-600 pl-4">Terminale – Cours & Exercices</a>
            <a href="/terminale/annales-bac" class="block text-sm py-1 text-gray-600 pl-4">BAC – Annales</a>
          </div>
        </div>
        
        <a href="/tarifs" class="nav-link-mobile block text-sm py-2">
          Tarifs
        </a>

        <!-- Enseignement supérieur Mobile -->
        <a href="/enseignement-python" class="nav-link-mobile block text-sm py-2">
          Python
        </a>
      </div>
    </div>
  </nav>
</header>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  
  menuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });

  // Function to position panel and caret
  function positionPanelAndCaret(panel, triggerId, beforeShow = false) {
    const header = document.getElementById('main-header');
    const trigger = document.getElementById(triggerId);
    const caret = panel.querySelector('.mega-menu-caret');
    
    if (!header || !trigger || !caret) return;

    // If positioning before show, panel is already in DOM but hidden
    // Just ensure it's positioned correctly before making visible
    if (beforeShow) {
      // Keep transform for centering
      panel.style.left = '50%';
      panel.style.transform = 'translateX(-50%)';
    }

    // Force a reflow to ensure dimensions are calculated
    void panel.offsetHeight;

    // Calculate header bottom position
    const headerRect = header.getBoundingClientRect();
    const headerBottom = headerRect.bottom;
    
    // Position panel below header
    panel.style.top = `${headerBottom + 12}px`; // 12px = mt-3
    
    // Force another reflow after setting top
    void panel.offsetHeight;
    
    // Calculate trigger center position relative to panel
    const triggerRect = trigger.getBoundingClientRect();
    const panelRect = panel.getBoundingClientRect();
    
    // Calculate trigger center position relative to panel
    const triggerCenterX = triggerRect.left + triggerRect.width / 2;
    const panelLeft = panelRect.left;
    
    // Position caret to point at trigger center
    const caretLeft = triggerCenterX - panelLeft;
    caret.style.left = `${Math.max(24, Math.min(caretLeft, panelRect.width - 24))}px`;

    // If positioning before show, positioning is done
    // The hidden class will be removed after this function returns
    // which will make it visible with the correct position already set
  }

  // Mega Menu functionality
  const megaMenuGroups = document.querySelectorAll('.mega-menu-group');
  let hoverTimeout = null;
  let isHovering = false;

  megaMenuGroups.forEach((group) => {
    const trigger = group.querySelector('.mega-menu-trigger');
    const panel = group.querySelector('.mega-menu-panel');
    const chevron = group.querySelector('.mega-menu-chevron');
    const triggerId = panel?.getAttribute('data-trigger-id');
    
    if (!trigger || !panel || !triggerId) return;

    // Click handler
    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = !panel.classList.contains('hidden');
      
      // Close all other menus
      megaMenuGroups.forEach((otherGroup) => {
        if (otherGroup !== group) {
          const otherPanel = otherGroup.querySelector('.mega-menu-panel');
          const otherTrigger = otherGroup.querySelector('.mega-menu-trigger');
          const otherChevron = otherGroup.querySelector('.mega-menu-chevron');
          if (otherPanel) otherPanel.classList.add('hidden');
          if (otherTrigger) otherTrigger.classList.remove('mega-menu-active');
          if (otherChevron) otherChevron.classList.remove('rotate-180');
        }
      });
      
      // Toggle current menu
      if (isOpen) {
        panel.classList.add('hidden');
        trigger.classList.remove('mega-menu-active');
        if (chevron) chevron.classList.remove('rotate-180');
      } else {
        // Position panel and caret BEFORE showing it
        positionPanelAndCaret(panel, triggerId, true);
        // Remove hidden class after positioning
        panel.classList.remove('hidden');
        trigger.classList.add('mega-menu-active');
        if (chevron) chevron.classList.add('rotate-180');
      }
    });

    // Hover handlers
    group.addEventListener('mouseenter', () => {
      if (hoverTimeout) {
        clearTimeout(hoverTimeout);
        hoverTimeout = null;
      }
      isHovering = true;
      
      // Close other menus
      megaMenuGroups.forEach((otherGroup) => {
        if (otherGroup !== group) {
          const otherPanel = otherGroup.querySelector('.mega-menu-panel');
          const otherTrigger = otherGroup.querySelector('.mega-menu-trigger');
          const otherChevron = otherGroup.querySelector('.mega-menu-chevron');
          if (otherPanel) otherPanel.classList.add('hidden');
          if (otherTrigger) otherTrigger.classList.remove('mega-menu-active');
          if (otherChevron) otherChevron.classList.remove('rotate-180');
        }
      });
      
      // Position panel and caret BEFORE showing it
      positionPanelAndCaret(panel, triggerId, true);
      // Remove hidden class after positioning
      panel.classList.remove('hidden');
      trigger.classList.add('mega-menu-active');
      if (chevron) chevron.classList.add('rotate-180');
    });

    group.addEventListener('mouseleave', () => {
      isHovering = false;
      hoverTimeout = setTimeout(() => {
        if (!isHovering) {
          panel.classList.add('hidden');
          trigger.classList.remove('mega-menu-active');
          if (chevron) chevron.classList.remove('rotate-180');
        }
      }, 150);
    });
  });

  // Close menus when clicking outside
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.mega-menu-group')) {
      megaMenuGroups.forEach((group) => {
        const panel = group.querySelector('.mega-menu-panel');
        const trigger = group.querySelector('.mega-menu-trigger');
        const chevron = group.querySelector('.mega-menu-chevron');
        if (panel) panel.classList.add('hidden');
        if (trigger) trigger.classList.remove('mega-menu-active');
        if (chevron) chevron.classList.remove('rotate-180');
      });
    }
  });

  // Close menus when clicking on a link inside
  megaMenuGroups.forEach((group) => {
    const panel = group.querySelector('.mega-menu-panel');
    if (panel) {
      panel.addEventListener('click', (e) => {
        const target = e.target;
        if (target.tagName === 'A' || target.closest('a')) {
          const trigger = group.querySelector('.mega-menu-trigger');
          const chevron = group.querySelector('.mega-menu-chevron');
          panel.classList.add('hidden');
          if (trigger) trigger.classList.remove('mega-menu-active');
          if (chevron) chevron.classList.remove('rotate-180');
        }
      });
    }
  });

  // Mobile dropdowns
  const mobileDropdowns = [
    { button: 'mobile-dropdown-cours-button', menu: 'mobile-dropdown-cours' }
  ];

  mobileDropdowns.forEach(({ button, menu }) => {
    const buttonEl = document.getElementById(button);
    const menuEl = document.getElementById(menu);
    
    if (buttonEl && menuEl) {
      buttonEl.addEventListener('click', (e) => {
        e.stopPropagation();
        const isHidden = menuEl.classList.contains('hidden');
        
        // Close other mobile dropdowns
        mobileDropdowns.forEach(({ menu: otherMenu, button: otherButtonId }) => {
          if (otherMenu !== menu) {
            document.getElementById(otherMenu)?.classList.add('hidden');
            const otherButton = document.getElementById(otherButtonId);
            if (otherButton) {
              const otherArrow = otherButton.querySelector('svg');
              if (otherArrow) {
                otherArrow.style.transform = 'rotate(0deg)';
              }
            }
          }
        });
        
        menuEl.classList.toggle('hidden');
        
        // Rotate arrow
        const arrow = buttonEl.querySelector('svg');
        if (arrow) {
          arrow.style.transform = isHidden ? 'rotate(180deg)' : 'rotate(0deg)';
        }
      });
    }
  });

  // Scroll handler to modify navbar and show CTA
  const header = document.getElementById('main-header');
  const headerCta = document.getElementById('header-cta');
  let lastScrollY = window.scrollY;
  const scrollThreshold = 50; // Show CTA after scrolling 50px

  const handleScroll = () => {
    const currentScrollY = window.scrollY;
    
    if (currentScrollY > scrollThreshold) {
      // Add scrolled class to header
      header?.classList.add('scrolled');
      
      // Show CTA button
      if (headerCta) {
        headerCta.classList.remove('opacity-0', 'translate-x-4');
        headerCta.classList.add('opacity-100', 'translate-x-0');
      }
    } else {
      // Remove scrolled class from header
      header?.classList.remove('scrolled');
      
      // Hide CTA button
      if (headerCta) {
        headerCta.classList.add('opacity-0', 'translate-x-4');
        headerCta.classList.remove('opacity-100', 'translate-x-0');
      }
    }
    
    lastScrollY = currentScrollY;
  };

  // Throttle scroll events for better performance
  let ticking = false;
  window.addEventListener('scroll', () => {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        handleScroll();
        ticking = false;
      });
      ticking = true;
    }
  });

  // Check initial scroll position
  handleScroll();

  // Update panel positions on scroll and resize
  window.addEventListener('scroll', () => {
    megaMenuGroups.forEach((group) => {
      const panel = group.querySelector('.mega-menu-panel');
      const triggerId = panel?.getAttribute('data-trigger-id');
      if (panel && !panel.classList.contains('hidden') && triggerId) {
        positionPanelAndCaret(panel, triggerId);
      }
    });
  });

  window.addEventListener('resize', () => {
    megaMenuGroups.forEach((group) => {
      const panel = group.querySelector('.mega-menu-panel');
      const triggerId = panel?.getAttribute('data-trigger-id');
      if (panel && !panel.classList.contains('hidden') && triggerId) {
        positionPanelAndCaret(panel, triggerId);
      }
    });
  });
</script>

<style>
  /* Logo animation styles */
  .logo-link {
    transition: all 0.3s ease;
  }

  .logo-link:hover {
    transform: scale(1.05);
  }

  .logo-text {
    color: #1e40af; /* blue-800 */
    transition: color 0.3s ease;
  }

  .logo-link:hover .logo-text {
    color: #2563eb; /* blue-600 */
  }

  .logo-image {
    animation: bounce-soft 2s ease-in-out infinite;
  }

  @keyframes bounce-soft {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  /* Navigation links with animated underline */
  .nav-link {
    position: relative;
    color: #475569; /* slate-600 */
    font-weight: 500;
    transition: all 0.3s ease;
    padding: 0.5rem 0;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: #2563eb; /* blue-600 */
    transition: width 0.3s ease;
  }

  .nav-link:hover {
    color: #2563eb; /* blue-600 */
    transform: translateY(-2px);
  }

  .nav-link:hover::after {
    width: 100%;
  }

  /* Mobile navigation links */
  .nav-link-mobile {
    position: relative;
    color: #475569; /* slate-600 */
    transition: all 0.3s ease;
    padding-left: 0.5rem;
    border-left: 2px solid transparent;
  }

  .nav-link-mobile:hover {
    color: #2563eb; /* blue-600 */
    border-left-color: #2563eb;
    transform: translateX(4px);
  }

  /* Scrolled state styles */
  header.scrolled {
    background: linear-gradient(to bottom, rgba(239, 246, 255, 0.98), rgba(255, 255, 255, 0.98));
    box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.1), 0 2px 4px -1px rgba(59, 130, 246, 0.05);
    border-bottom-color: rgba(59, 130, 246, 0.2);
  }

  /* CTA button styles */
  #header-cta {
    white-space: nowrap;
  }

  /* Mega Menu Styles */
  .mega-menu-trigger {
    position: relative;
  }

  .mega-menu-trigger.mega-menu-active {
    color: #2563eb; /* blue-600 */
    background-color: rgba(239, 246, 255, 0.8); /* blue-50 with opacity */
    border-radius: 0.5rem;
    padding: 0.5rem 0.75rem;
    margin: -0.5rem -0.75rem;
  }

  .mega-menu-chevron {
    transition: transform 0.2s ease;
  }

  .mega-menu-chevron.rotate-180 {
    transform: rotate(180deg);
  }

  .mega-menu-panel {
    animation: fadeInDown 0.2s ease-out;
    contain: layout style paint;
    backface-visibility: hidden;
    -webkit-font-smoothing: antialiased;
  }
  
  .mega-menu-panel.hidden {
    visibility: hidden !important;
    opacity: 0 !important;
    pointer-events: none !important;
    display: block !important;
  }
  
  /* Ensure smooth appearance without shift */
  .mega-menu-panel:not(.hidden) {
    will-change: opacity;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .mega-menu-caret {
    position: absolute;
    top: -8px;
    width: 0;
    height: 0;
    border-left: 8px solid transparent;
    border-right: 8px solid transparent;
    border-bottom: 8px solid white;
    filter: drop-shadow(0 -2px 2px rgba(0, 0, 0, 0.05));
    z-index: 1;
    transition: left 0.2s ease;
  }

  .mega-menu-link {
    text-decoration: none;
  }

  .mega-menu-card {
    text-decoration: none;
  }

  /* Ensure mega menu panel doesn't overflow on mobile */
  @media (max-width: 768px) {
    .mega-menu-panel {
      position: fixed;
      left: 1rem;
      right: 1rem;
      width: auto;
      min-width: auto;
      transform: none;
    }
    
    .mega-menu-panel > div {
      min-width: auto;
    }
    
    .mega-menu-caret {
      left: 24px !important;
    }
  }

  /* Adjust panel position on smaller desktop screens if needed */
  @media (max-width: 1200px) {
    .mega-menu-panel {
      max-width: calc(100vw - 2rem);
    }
  }
</style>
